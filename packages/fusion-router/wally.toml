[package]
name = "stackfox-labs/fusion-router"
description = "A navigation and screen management library for Roblox UI built with Fusion."
license = "MIT"
authors = ["Elttob"]
version = "0.1.0"

registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

exclude = ["**"]
include = ["src", "src/*", "default.project.json", "wally.lock", "wally.toml"]

[dependencies]
Fusion = "elttob/fusion@0.3.0"
